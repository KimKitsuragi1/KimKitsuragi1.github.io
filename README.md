# Revachol Review Visual Forced Site

This package uses embedded CSS in every HTML page. It should render correctly even if GitHub Pages does not load external CSS.

Upload all files in this directory directly to the root of `KimKitsuragi1.github.io`.
